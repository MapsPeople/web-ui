import { RouteInstructionsHeading } from './route-instructions-heading';

describe('mi-route-instructions-heading', () => {
    it('builds', () => {
        expect(new RouteInstructionsHeading()).toBeTruthy();
    });
});
