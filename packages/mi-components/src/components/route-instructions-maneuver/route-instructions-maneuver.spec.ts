import { RouteInstructionsManeuver } from './route-instructions-maneuver';

describe('mi-route-instructions-maneuver', () => {
    it('builds', () => {
        expect(new RouteInstructionsManeuver()).toBeTruthy();
    });
});
